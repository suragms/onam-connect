# Google Gemini API key for Vercel serverless AI endpoints (api/ai/*).
# Never expose this key in frontend code or commit it to git.
# Local: copy to .env (or .env.local) and set the value.
# Vercel: Project Settings → Environment Variables → GEMINI_API_KEY
GEMINI_API_KEY=
